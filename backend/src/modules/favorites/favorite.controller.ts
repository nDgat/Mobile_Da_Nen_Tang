import type { NextFunction, Request, Response } from "express";
import { createFavorite, deleteFavorite, FavoriteNotFoundError, FavoriteValidationError, favoriteStatus, listFavorites } from "./favorite.service.js";
function handle(error: unknown, response: Response) { if (error instanceof FavoriteValidationError) { response.status(400).json({ error: { code: "VALIDATION_ERROR", message: error.message } }); return true; } if (error instanceof FavoriteNotFoundError) { response.status(404).json({ error: { code: "MOVIE_NOT_FOUND", message: error.message } }); return true; } return false; }
export async function listFavoriteHandler(req: Request, res: Response, next: NextFunction) { try { res.json(await listFavorites(res.locals.auth.userId as number, req.query as Record<string, unknown>)); } catch (error) { if (!handle(error, res)) next(error); } }
export async function favoriteStatusHandler(req: Request, res: Response, next: NextFunction) { try { res.json({ data: await favoriteStatus(String(req.params.movieId), res.locals.auth.userId as number) }); } catch (error) { if (!handle(error, res)) next(error); } }
export async function createFavoriteHandler(req: Request, res: Response, next: NextFunction) { try { res.status(201).json({ data: await createFavorite(String(req.params.movieId), res.locals.auth.userId as number) }); } catch (error) { if (!handle(error, res)) next(error); } }
export async function deleteFavoriteHandler(req: Request, res: Response, next: NextFunction) { try { res.json({ data: await deleteFavorite(String(req.params.movieId), res.locals.auth.userId as number) }); } catch (error) { if (!handle(error, res)) next(error); } }
